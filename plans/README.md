# Implementation Plans

Generated by the improve skill on 2026-07-06 against commit `1ebd544`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions. The reviewer
(advisor session) maintains this index.

Execution model: sequential executors in isolated git worktrees; each worktree
merges the previous plan's branch before starting, so later plans see earlier
work. Nothing is merged to `main` — that is the maintainer's decision at the
end.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix EXIF orientation mismatch (crops vs boxes) | P1 | S | — | DONE (branch `worktree-agent-a3f575cef40fcaf3f`, commit b44810b) |
| 002 | Make configs/default.yaml the real source of defaults | P1 | S | 001 (shared file) | DONE (branch `worktree-agent-aeace3f7e4113935d`, commit 4a11754; note: 0.07 sweep point in check_acne04_detector.py:101 is intentional, not a default) |
| 003 | Test the rules engine + YOLO geometry | P1 | M | — | DONE (branch `worktree-agent-a80c72cf71ff27d24`, commit 78f5e22) |
| 004 | Bridge-readiness: unify concern vocabulary | P1 | S | 002, 003 | DONE (branch `worktree-agent-a783a637999cd7a8d`, commit 94fe9eb) |
| 005 | Class weights derived from data dir | P2 | S | 002 (shared file) | DONE (branch `worktree-agent-a68e817fcdaa1dd90`, commit 04c1ba7) |
| 006 | Batch crop classification | P2 | S | 004 (shared file) | DONE (branch `worktree-agent-a1bf968dff9b36d8c`, commit b0416fb) |
| 007 | Dedupe require / VOC parsing / metadata helpers | P3 | S | 006 (shared files) | DONE (branch `worktree-agent-afb40e780e5cb20e6`, commit d552dd8) |
| 008 | DECISIONS D-018 → shipped YOLOv8m (docs only) | P3 | S | — | DONE (branch `worktree-agent-a3273d8e761bb26e9`, commit e50a676) |
| 009 | Restore Stage 1 training notebook from history | P2 | S | — | DONE (branch `worktree-agent-a830f161820f39d1f`, commit 5e25a4d) |
| 010 | Design doc: not_acne negative class (D1) | P2 | S | 004 | DONE (branch `worktree-agent-a006e650069606cd5`, commit d19f3b6; note: sizing target left as `OPEN:` — read live counts via `--inspect` at execution, better than the stale pre-005 hardcode) |
| 011 | Catalog importer: CSV → catalog.json (D2) | P2 | M | 003 | DONE (branch `worktree-agent-a2abb7e89293ef36a`, commit afab600) |
| 012 | Design doc: Fitzpatrick-disaggregated eval (D3) | P3 | S | — | DONE (branch `worktree-agent-ae7ecafeb3c2df806`, commit a04591e) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

**All 12 plans executed and reviewed (2026-07-07).** Each plan's branch merges
the previous one, so the tip branch `worktree-agent-ae7ecafeb3c2df806` contains
the entire chain (12 commits, 25 files, all 9 test files + classifier
self-check green). To land everything:
`git merge worktree-agent-ae7ecafeb3c2df806` from `main` — maintainer's call.

## Dependency notes

- 002 follows 001 because both edit `run_acne04_pipeline.py` (defaults vs
  loader); sequential worktree chaining avoids a merge conflict.
- 004 needs 002 (adds a key to the now-live YAML) and 003 (engine tests are
  the safety net for vocabulary work).
- 005 follows 002 (same trainer file).
- 006 follows 004 (same classifier file); 007 follows 006 (same files again).
- 010 reflects post-004 vocabulary; 011 uses 003's engine tests as its
  integration safety net.
- **Deferred by maintainer decision (2026-07-06):** the full Stage 2→Stage 3
  bridge (ConcernReport construction, region assignment, severity derivation).
  Plan 004 is the preparation for it; the bridge itself will be planned when
  the maintainer picks it back up.

## Findings considered and rejected

- `assert`-based validation in `schema.py` (stripped under `python -O`): fine
  for a learning project; revisit only if this ever serves untrusted input.
- Unpinned `requirements.txt`: deliberate per its own comment; tighten only
  if reproducing the T4 training numbers starts to matter.
- Unused pandas/matplotlib in requirements: Colab-side leftovers; harmless.
- Merging the two sheet renderers (`draw_sheet` / `render_sheet`): different
  outputs, low payoff — noted in plan 007 as deliberately skipped.
- Security audit: no findings — no web surface, no secrets, local CLI only.
- `runs/` artifact accumulation: local-only and gitignored; not worth tooling.
