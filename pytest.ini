[pytest]
testpaths = tests recsys/tests agentrec/tests
addopts = -m "not real_models"
markers =
    real_models: slow checks that need local model/image artifacts
    raw_dump: checks that need the local Kaggle Sephora dump under data/raw/sephora
